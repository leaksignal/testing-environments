serviceMonitor:
  enabled: true
  interval: 1s


policies:
  agent:
    categories:
      bank_routing_us:
      - internal: routing_number
    endpoints:
      - matches: "**"
        config:
          bank_routing_us: {}